import Hero from '@/components/landing/Hero';

export default function LandingPage() {
  return (
    <main>
      <Hero />
      {/* You'll add Features, HowItWorks, CTA, Footer components here later */}
    </main>
  );
}